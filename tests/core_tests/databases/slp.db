# DNS database for the .slp top level domain

@ DEFAULT slp.
TTL DEFAULT 60

@ SOASP sdt.slp. TTL
@ SOAADMIN dns\.admin.slp. TTL

# This parameter might change in order to test zone transfers
@ SOASERIAL 20221123 TTL

@ SOAREFRESH 30 TTL
@ SOARETRY 30 TTL
@ SOAEXPIRE 40 TTL

mindflayer NS sp.mindflayer.slp. TTL 1
mindflayer NS ss2.mindflayer.slp. TTL 2
mindflayer NS ss1.mindflayer.slp. TTL 3

djinn CNAME sheoldred.batata TTL

mindflayer A 10.0.12.11 TTL

sp.mindflayer.slp. A 10.0.13.10 TTL
ss1.mindflayer.slp. A 10.0.13.11 TTL
ss2.mindflayer.slp. A 10.0.13.12 TTL