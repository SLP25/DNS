# DNS database for the .slp top level domain

@ DEFAULT mindflayer.slp.
TTL DEFAULT 60

@ SOASP sp.mindflayer.slp. TTL
@ SOAADMIN dns\.admin.mindflayer.slp. TTL

# This parameter might change in order to test zone transfers
@ SOASERIAL 20221123 TTL

@ SOAREFRESH 30 TTL
@ SOARETRY 30 TTL
@ SOAEXPIRE 40 TTL

www A 10.0.12.11 TTL

exile CNAME foretell TTL

mail MX foretell TTL 10
mail MX blue TTL 20

foretell A 10.0.12.16 TTL
blue A 10.0.12.17 TTL